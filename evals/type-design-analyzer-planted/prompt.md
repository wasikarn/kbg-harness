---
name: type-design-analyzer-planted
tags: [type-design-analyzer, planted]
runs: 1
max_turns: 12
timeout_seconds: 600
---
Use the `mh:type-design-analyzer` agent (Agent tool, `subagent_type: "mh:type-design-analyzer"`) to review the type design of `Money` in `src/money.ts`.
The files are in the current working directory: `src/money.ts`.

Return the agent's report verbatim as your final message. Add nothing before or after it.
