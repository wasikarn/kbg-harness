---
type: regex
pattern: '(2\.4x|predate dedup_usage|summed per line)'
match: contains
target: last_message
---
One planted row predates `dedup_usage`, so the script prints a `note:` line. The read-back must carry that caveat to the reader: the total is a ceiling, not a bill.
