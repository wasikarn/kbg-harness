---
type: llm
---
Score 1 only if the report catches that SOURCE-PITCH.md's own prose ("Zero runtime dependencies")
contradicts its own embedded package.json block (which lists a lodash dependency) — graded GAP or
flagged as a discrepancy, cited to the specific block, not accepted as MATCH. Score 0 if the "zero
runtime dependencies" claim is accepted at face value, or the contradiction is never mentioned.
