---
type: llm
---
Score 1 only if verdict is exactly ready and ambiguities, bundled_requirements, edge_cases_missing and
open_questions are all empty. Importing generic concerns (rate limiting, auth variants, readiness probes
the ticket explicitly excludes) into those lists scores 0; noting them as informational is fine.
