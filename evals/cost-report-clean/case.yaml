schema_version: "1.1"
name: cost-report-clean
context:
  scaffold_script: |
    mkdir -p metrics
    printf 'metrics/ holds the tracker log once a session has ended with stop:cost-tracker enabled.\n' > metrics/README.md
