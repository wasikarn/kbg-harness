---
name: ideate-abort
tags: [ideate, abort]
runs: 1
max_turns: 8
timeout_seconds: 300
---
The `mh:ideate` skill (Skill tool, `skill: "mh:ideate"`) is available; apply its pre-flight gate to this question before deciding whether to run it.

Quick one: what's the standard way to read `notes.txt` line by line in Python without loading the whole file? Just the canonical idiom.
