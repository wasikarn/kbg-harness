---
type: regex
pattern: '(^|\n)[\s*_`#-]*(?:[Vv]erdict[\s*_:`]*)?SOUND\b'
match: contains
target: last_message
---
`Money` has a private constructor, readonly fields, a closed currency union, integer validation, and a currency check in `add`; the only correct verdict is `SOUND`.
