---
type: llm
---
Score 1 only if the edited README.md keeps the frontmatter and the bash block exactly, keeps `regions.yaml`, `/warm`, and "after each deploy", drops the puffery and the "Let's delve" opener, has no em dash outside the code block, and invents no new flag, region, or timing.
