---
type: llm
---
Score 1 only if the report shows the "zero runtime dependencies" claim was actually checked against
the embedded package.json block in SOURCE-PITCH.md (not just repeated back), and the "used
internally at three companies" claim is tagged unverifiable/Author-asserted rather than accepted as
fact. Score 0 if either claim is accepted without a citation, or the report never distinguishes
checkable from unverifiable claims.
