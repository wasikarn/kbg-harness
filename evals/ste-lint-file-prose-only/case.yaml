schema_version: "1.1"
name: ste-lint-file-prose-only
context:
  scaffold_script: |
    cat > mixed.md <<'FIXTURE_EOF'
    # Notes

    Do not use this method; it is deprecated.

    ```bash
    echo "a;b"
    ```
    FIXTURE_EOF
